---
'@wireweave/core': minor
---

feat: expose the shared anchor navigation intent and classify URL-shaped `navigate` targets as external transitions.

fix: align the legacy `module` entrypoint with the published ESM file declared by `exports.import`.
